"use server";
import { revalidatePath } from "nest/cache";
import { redirect } from "next/navigation";

import { createClient } from "@/utils/supabase/server";

export async function  createPost(postText) {
    try {
        const supabase = 
    }
}